apiVersion: apps/v1
kind: Deployment
metadata:
  name: hello-web
  namespace: demo
spec:
  replicas: 2
  selector:
    matchLabels:
      app: hello-web
  template:
    metadata:
      labels:
        app: hello-web
    spec:
      containers:
      - name: hello-web
        image: nginx:1.27
        ports:
        - containerPort: 80
